export class doctor {
  firstName: string;
  lastName: string;
  fileNumber: string;
  constructor(firstName: string, lastName: string, fileNumber: string)
  {
	this.firstName = firstName;
	this.lastName = lastName;
	this.fileNumber = fileNumber;
  }
}
